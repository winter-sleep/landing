/**
 * 提交数据结构
 */
export interface SigninData {
    email: string;
    password: string;
}

export interface FieldState {
    email: Boolean;
    password: Boolean;
}

export interface MessageBox {
    email: string;
    password: string;
}
